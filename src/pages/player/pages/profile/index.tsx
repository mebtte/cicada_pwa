import React from 'react';
import styled from 'styled-components';

import Page from '../page';

const Style = styled(Page)``;

const Profile = () => {
  return <Style>profile</Style>;
};

export default Profile;
