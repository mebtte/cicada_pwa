import styled from 'styled-components';

export default styled.div`
  padding: 10px 20px;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 20px;
  > .left {
    flex: 1;
    display: flex;
    align-items: center;
    gap: 20px;
  }
`;
